/* First step
    Create script add that takes a String and returns a String and use console.log() to show the result.
*/

let first = "13";
let second = "21";

console.log((parseInt(first, 10) + parseInt(second, 10)).toString());

/*
 * Ou sinon, nous pouvons aussi faire ça : 
 * 
 * let first = parseInt("13", 10);
 * let second = parseInt("21", 10);
 * 
 * console.log(`$(first + second)`); 
 */